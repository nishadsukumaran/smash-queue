# Local SQLite file (default). Nothing else is required to run locally.
DATABASE_URL=./data/smash.db

# Secret used to sign session QR check-in tokens.
QR_SECRET=change-me-before-you-share-a-real-session

# Base URL used when rendering shareable links and QR codes.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
